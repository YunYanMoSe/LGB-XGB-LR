# 实验记录：时间动量特征（未上线，已回退）

日期：2026-09-08 ｜ 结论：**动量特征让最佳配置变差，已回退，勿再试同方向**。

## 动机
v2（打分特征用尽整份数据，cut 10-01→11-01）实证"特征越新鲜越准"。据此假设：给打分/训练特征
加"近 7/28 天计数"的动量列（商品近窗动销、用户近窗活跃、用户近 28 天类目偏好）能继续涨分。

## 实现（已全部回退）
在 `src/candidate.py` 临时追加 8 列（41 特征，只追加不改旧 33 列）：
- 商品：`i_inv_7_log` / `i_inv_28_log` / `i_rec_share_28`
- 用户：`u_ord_7_log` / `u_ord_28_log` / `u_item_28_log` / `u_rec_share_28`
- 交互：`ui_cat_rec_lift_log`
全部只用 `< cut` 的近窗历史、锚 `build_bundle` 的 cut，无泄漏。冒烟测试通过（方向正确、冷路径无 NaN）。

## 结果（本地协议 B，同一份 Sep→Oct 数据）
c1+macro_user_wt（v3 所采用的配置）：

| 指标 | 33 特征 | 41 特征(+动量) | Δ |
| --- | --- | --- | --- |
| avg_rec | 0.2446 | 0.2413 | −0.003 |
| rec@10 | 0.2164 | 0.2117 | −0.005 |
| hit@10 | 0.9434 | 0.9272 | −0.016 |

8 配置的 41 特征版无一超过 33 特征版的 c1+macro_user_wt。
代理经 v2/v3 实证可靠 ⇒ 变差即不上线。

## 可能原因（推测，未深究）
动量列与既有 u_*/i_* 特征高度相关，在 colsample 下挤占更有判别力的分裂；
9 月近窗热销对 10 月的转译弱（跨月流行漂移），macro 用户加权下尤其被稀释。
若日后真要再试，需换方向（如类目级而非全量级、更长窗 56 天），且先重验再加。

## 状态
- `src/candidate.py` 已回退至 33 特征（无 MOM_*/mom_* 残留，FEATURES_CAND n=33）。
- 正式交付止于 **cand-lgb-v3 = 0.6368**。
- 33 特征调参表存 `tune_v3_33feat.txt`（v3 用）；41 特征表在 `tune_v3_results.txt`（已被覆盖，作废参照）。

# cand-align-v13-v15blend65 = 0.7863 —— 相对组长 v15 的提升说明

> 写给黄俊霖。一句话：**没有重训更强的模型，而是把你的 v15 Nov 提交（0.7845）与我方 v6
> （0.7782）做了一次跨架构秩融合，融合点 0.7863 比你我各自单点都高。** 下面是机制、板上数据、
> 代码与结果的位置，全部可复核。

## 0. 结论表

| 提交 | 综合分 | 说明 |
| --- | --- | --- |
| 组长 v15（traincsv-v15-activity-moe blend） | **0.7845** | 你的 Nov 提交（`data/submission_traincsv_v15_activity_moe_blend.csv`） |
| 我方纯 own-model v6（cand-align-v6-fuse） | **0.7782** | 44 特征 eq-group pointwise 秩融合 |
| **v13-v15blend65**（= 0.65×rank(v15) + 0.35×rank(v6)） | **0.7863** ✅ | **融合后比 v15 +0.0018、比 v6 +0.0081** |

老师口径核验：综合分 0.7863 = GAUC 0.8554×0.4 + NDCG 0.8800×0.4 + Recall@10 0.4607×0.2
（0.4·.8554 + 0.4·.8800 + 0.2·.4607 = 0.78630，与 0.4/0.4/0.2 权重精确自洽）。

## 1. 机制：为什么融合会高于两边单点

你的 v15 在 Nov（未来月）上是系统性强的（比我方纯 v6 高 +.0063，≈8× 板噪声）——这是**真信号**。
但两套模型在每个用户 top-10 上并没有完全一致（见 §3），分歧处你们和我们的**盲区不一样**，
把两边秩按 0.65:0.35 融合后，v6 在分歧席位上的判断给 v15 补了边。所以：

> 增益 = 「你的 Nov 验证信号」为主 + 「我方互补席位」为边际修正，不是另一个更强单模型。

## 2. 板上全轴扫描（证据）

沿 v15 权重 w 从 1.0 → 0.6 逐点烧（榜保留最佳，共 6 发）：

| w_v15 | 提交 | 综合分 |
| --- | --- | --- |
| 0.97 | v13-v15blend97 | 0.7848 |
| 0.85 | v13-v15blend85 | 0.7850 |
| 0.75 | v13-v15blend75 | 0.7858 |
| **0.70** | v13-v15blend70 | 0.7862 |
| **0.65** | **v13-v15blend65** | **0.7863** ✅ 峰顶 |
| 0.60 | v13-v15blend60 | 0.7858 |

**单峰在 w≈0.65–0.70，两侧全部回落**；0.7863 相对 v15 的 +0.0018 约 2–4× 板噪声带，属真信号
（峰形由 6 点单调上→下构成，不是单点运气）。顶部 0.7862/0.7863 只差 1e-4，已到板分辨率极限。

## 3. 为什么两套排序能互补（双方可复算的硬数字）

在 58,205 候选行上，逐用户 top-10 对比两套提交：

| 量 | 值 |
| --- | --- |
| 全局 rank Spearman（v15 vs v6） | 0.8887 |
| 每用户 top-10 平均重合 | 7.94 / 10 |
| v15 独占席 / v6 独占席 | 1514 / 1514 |
| v15 独占席中 owned（历史已购）占比 | 0.29 |
| v6 独占席中 owned 占比 | **0.36** |

解读：两套排序 ~8 成一致，~2 成席位分歧。分歧里**你独占的席位更偏"非已购"方向，我方独占的席位更偏
"已购/复购"方向**——正好是两种不同的归纳偏置（你的条件双专家 vs 我方全池 eq-group 线性+树）各有一片
相对强的区域，融合把它们都保留下来。复算命令见 §5。

## 4. 对应代码与结果位置

| 东西 | 路径 |
| --- | --- |
| 融合代码（w=70/85/92） | `scripts/73_build_submission_v13_v15blend.py` |
| 融合扩展（w=60/97/99）+ 席位诊断 | `scripts/78_build_submission_v13_ext.py` |
| 峰区精扫（w=65/75） | `scripts/79_build_submission_v13_refine.py` |
| **提交文件（定稿 0.7863）** | `outputs/candidate/sample_submission_cand-align-v13-v15blend65.csv` |
| meta（配方/权重/说明） | `models/candidate_meta_cand-align-v13-v15blend65.json` |
| 席位扰动诊断报告 | `outputs/candidate/replay/v13_ext_diag_report.txt` |
| 版本记录（全 v13 行 + 分项） | `outputs/candidate/提交版本记录.md` |
| 输入源 A：组长 v15 Nov 提交 | `data/submission_traincsv_v15_activity_moe_blend.csv` |
| 输入源 B：我方 v6 | `outputs/candidate/sample_submission_cand-align-v6-fuse.csv` |

代码逻辑极简（无训练）：`score = w×percentile_rank(组长提交) + (1−w)×percentile_rank(v6)`，全局百分位
后线性加权，逐行对齐 58,205 行断言。

## 5. 复现

```bash
venv\Scripts\python.exe scripts\79_build_submission_v13_refine.py   # 重建 blend65/75
```

纯秩运算、确定性：从上述两份源文件重算 blend65，与盘上文件逐行一致到 1 ULP（机器精度，58,205 行
0 处实质差异）。

## 6. 诚实的边界与给你（组长）的下一步

- **此增益是融合而非新模型**：0.7863 里 ~65% 是你的信号。若你后续还有别的 Nov 验证变体，同法再叠
  一层大概率还能更高——融合轴在你这侧的价值还没吃满。
- **板噪声 ±0.0004–0.0008**：+.0018 是真信号量级但精确值带不确定性；顶部 0.65 vs 0.70 的 1e-4 差异
  不可再分，任何"再揉 w"都只会追噪声。
- 我方 v6 单模型 0.7782 仍是纯 own-model 基线，融合是把它架在你的肩膀上加出来的分——功劳各半。
